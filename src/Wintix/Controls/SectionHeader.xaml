<UserControl
    x:Class="Wintix.Controls.SectionHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Wintix.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Spacing="6">
            <TextBlock Style="{StaticResource WintixPageTitleStyle}" Text="{x:Bind Title, Mode=OneWay}" />
            <TextBlock
                MaxWidth="720"
                Style="{StaticResource WintixSubtitleStyle}"
                Text="{x:Bind Subtitle, Mode=OneWay}" />
        </StackPanel>

        <Button
            Grid.Column="1"
            VerticalAlignment="Center"
            Click="ActionButton_Click"
            Content="{x:Bind ActionText, Mode=OneWay}"
            Style="{StaticResource WintixPrimaryButtonStyle}"
            Visibility="{x:Bind ShowAction, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
