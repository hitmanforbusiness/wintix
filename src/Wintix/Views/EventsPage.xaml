<Page
    x:Class="Wintix.Views.EventsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Wintix.Models"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource PageTitleStyle}" Text="Events" />
        <TextBlock
            Grid.Row="1"
            Style="{StaticResource SectionSubtitleStyle}"
            Text="Browse upcoming events and find your next experience." />

        <ProgressRing
            Grid.Row="2"
            HorizontalAlignment="Left"
            IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />

        <ListView
            Grid.Row="2"
            IsItemClickEnabled="True"
            ItemsSource="{x:Bind ViewModel.Events, Mode=OneWay}"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:EventItem">
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" />
                            <TextBlock Opacity="0.8" Text="{x:Bind Venue}" />
                            <TextBlock Opacity="0.7" Text="{x:Bind Category}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" Spacing="4">
                            <TextBlock HorizontalAlignment="Right" Text="{x:Bind FormattedDate}" />
                            <TextBlock HorizontalAlignment="Right" Text="{x:Bind FormattedPrice}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
